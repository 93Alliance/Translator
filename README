## 安装方法：
    解压后，在此目录下打开终端，输入sudo sh install.sh,然后等待安装。  
## 使用方法：
    本脚本有两条指令，分别为addword和fanyi  
    打开系统设置，选择键盘->快捷键->自定义快捷键,添加快捷键名称自定义，命令,组合键可以 自定义。
    此时打开浏览器，随意选择某个单词，或者中文，按中快捷键即可翻译。



## 支持：
    1. 支持单词英译汉
    2. 支持汉译英（字词）
    3. 支持整段句子翻译（英译汉或汉译英）
    4. 支持单词添加到本地列表，然后导入有道词典

## BUG:
    1.当句子末尾有'!'时，会出现bug，所以使用时，不要选择末尾带'!'。
